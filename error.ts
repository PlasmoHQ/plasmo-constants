/**
 * An enum of all Error Messages
 */
export enum ErrorMessage {
  Unknown = "Unknown error.",
  NotImplemented = "Not implemented (yet).",
  TooManyArg = "Too many arguments provided.",
  Standalone = "Must run from standalone binary."
}
